# Claude History Search Design Document

## Overview

Claude Code conversations are stored in .jsonl files with no way to search them semantically. Finding past discussions wastes tokens.

## Goal

Build a CLI for semantic search over Claude Code conversation history.
